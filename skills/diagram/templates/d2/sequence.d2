# 示例：API 登录鉴权时序图

shape: sequence_diagram

# --- 参与者（按调用链路从左到右排列）---

用户: {
  style: {
    fill: "#EFF6FF"
    stroke: "#93C5FD"
    font-color: "#1E293B"
    border-radius: 6
    font-size: 13
  }
}

前端: {
  style: {
    fill: "#ECFDF5"
    stroke: "#6EE7B7"
    font-color: "#1E293B"
    border-radius: 6
    font-size: 13
  }
}

API Gateway: {
  style: {
    fill: "#FFFBEB"
    stroke: "#FCD34D"
    font-color: "#1E293B"
    border-radius: 6
    font-size: 13
  }
}

Auth 服务: {
  style: {
    fill: "#F5F3FF"
    stroke: "#C4B5FD"
    font-color: "#1E293B"
    border-radius: 6
    font-size: 13
  }
}

MySQL: {
  shape: cylinder
  style: {
    fill: "#F8FAFC"
    stroke: "#CBD5E1"
    font-color: "#1E293B"
    font-size: 13
  }
}

Redis: {
  shape: cylinder
  style: {
    fill: "#FFF1F2"
    stroke: "#FDA4AF"
    font-color: "#1E293B"
    font-size: 13
  }
}

# --- 登录流程 ---

用户 -> 前端: 1. 输入账号密码
前端 -> API Gateway: 2. POST /login
API Gateway -> Auth 服务: 3. 转发请求
Auth 服务 -> MySQL: 4. 查询用户
MySQL -> Auth 服务: 5. 返回用户信息
Auth 服务 -> Auth 服务: 6. 验证密码 + 签发 Token
Auth 服务 -> Redis: 7. 存储 Session
Redis -> Auth 服务: 8. OK
Auth 服务 -> API Gateway: 9. 返回 Token
API Gateway -> 前端: 10. 200 OK
前端 -> 用户: 11. 登录成功
