# Deep-Research 报告质量自动检测 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 deep-research SKILL.md 中新增 Step 6 质量自检，报告写完后自动检测 4 个维度，可修项自动修复，不可修项列出供用户决定。

**Architecture:** 纯 prompt 驱动，修改 SKILL.md 一个文件。将现有"质量检查"章节（手动 checklist）替换为结构化的自动检测流程，包含检查规则、自动修复逻辑、终端输出格式。

**Tech Stack:** Markdown（SKILL.md prompt engineering）

## 关联

- 方案：`docs/specs/2026-04-02-deep-research-quality-check-design.md`

---

## 文件变更

- Modify: `skills/deep-research/SKILL.md:640-674` — 替换现有"质量检查"章节为新的 Step 6

---

### Task 1: 替换质量检查章节为 Step 6 自动检测框架

**Files:**
- Modify: `skills/deep-research/SKILL.md:640-674`

- [ ] **Step 1: 阅读现有质量检查章节确认替换范围**

读取 `skills/deep-research/SKILL.md` 第 640-674 行，确认要替换的完整内容。替换范围：从 `## 质量检查` 到文件末尾。

- [ ] **Step 2: 用 Edit 工具替换为新的 Step 6 章节**

将第 640 行 `## 质量检查` 到第 674 行文件末尾，替换为以下内容：

```markdown
## 第六步：质量自检

报告写完后，自动执行以下 4 项检查。可自动修复的问题直接修复，需要判断的列出供用户决定。

### 6.1 检查维度

#### 检查 1：结构完整性

读取报告全文，对照当前调研类型的模板，检查是否包含所有必备章节：

| 必备章节 | 检查方式 |
|---------|---------|
| Executive Summary | 存在 `## Executive Summary` 或等价标题，且内容 ≤500 字 |
| 现状基线 | 存在现状/基线相关章节（来自 `_common.md` 模板要求） |
| 正文主体 | 存在对应调研类型模板要求的核心分析章节 |
| 假设验证结果 | 存在 `## 假设验证结果` 且包含表格 |
| 反面观点与回应 | 存在 `## 反面观点与回应` 且非空 |
| 核心结论 | 存在结论章节，含带来源标注的结论条目 |
| 行动建议 | 存在建议/推荐章节 |
| 参考资料 | 存在 `## 参考资料` 且按 T1/T2/T3 分组 |

**处理方式**：不可自动修复。缺失章节在终端列出，提示用户是否补写。

#### 检查 2：引用格式

逐条扫描参考资料章节，检查：

- [ ] 参考资料是否按「一手来源 [T1]」「二手来源 [T2]」「三手来源 [T3]」三级分组
- [ ] 每条引用是否有 `[T1]`/`[T2]`/`[T3]` 层级标记
- [ ] 每条引用格式是否为 `[标题](URL) — 引用说明`
- [ ] 正文中引用关键数据时是否标注了来源编号

**处理方式**：可自动修复。
- 缺少分组 → 按来源类型自动归类到 T1/T2/T3
- 缺少层级标记 → 根据来源类型自动补充
- 格式不符 → 自动调整为标准格式
- 修复后在终端展示修复摘要

#### 检查 3：假设验证表

检查假设验证表的完整性：

- [ ] 报告中是否存在假设验证结果表格
- [ ] 第一步中提出的每个初始假设是否都在表中出现
- [ ] 每行是否包含三列：假设内容 | 验证结果（✅/❌/⚠️）| 关键证据及来源层级

**处理方式**：
- 表存在但个别行缺少结果标记或来源标注 → 自动补充格式占位（如 `⚠️ 待补充`），在终端提示
- 表不存在 → 在终端提示「缺少假设验证表」，询问用户是否根据全文内容生成

#### 检查 4：核心结论来源支撑

逐条检查「核心结论」章节中的每个结论：

- 该结论引用的证据来源层级是什么？
- 是否满足：**至少 1 个 T1 来源** 或 **至少 2 个独立 T2 来源**
- 仅有 T3 来源支撑的结论标记为不达标

**处理方式**：不可自动修复。在终端逐条列出不达标结论及其当前来源情况，询问用户是否补充搜索。

### 6.2 执行流程

```
报告写完
  ↓
读取报告全文，逐项执行 4 项检查
  ↓
自动修复可修项（检查 2 引用格式、检查 3 表格式）
  ↓
在终端输出检查摘要
  ↓
有不可修项？→ 列出问题，逐项询问用户处理意见
  ↓
用户决策（补写 / 补充搜索 / 跳过）→ 执行 → 完成
```

### 6.3 终端输出格式

检查完成后在终端输出以下格式的摘要（不写入报告文件）：

```
📋 报告质量自检
─────────────────────────────────────
✅ 结构完整性：8/8 必备章节齐全
🔧 引用格式：修复了 N 处问题（具体：补充了 X 个层级标记，调整了 Y 条格式）
✅ 假设验证表：N 个假设均有验证结果和证据来源
⚠️ 核心结论来源支撑：
   · "结论内容摘要A" → 仅 1 个 T3 来源，不达标
   · "结论内容摘要B" → 2 个独立 T2 来源，达标

总计：N 项通过，M 项需处理
─────────────────────────────────────
```

如果有不达标项，逐项询问：
```
⚠️ "结论内容摘要A" 仅有 T3 来源支撑。
   → 补充搜索以找到 T1/T2 来源？(y/n)
```

如果全部通过：
```
✅ 质量自检全部通过，报告已完成。
文件路径：<报告文件路径>
```
```

- [ ] **Step 3: 验证修改后 SKILL.md 结构完整性**

读取修改后的 SKILL.md，检查：
- 第一步到第六步标题连续完整
- 第六步内容格式正确、无 Markdown 语法错误
- 文件末尾有换行

Run: 手动读取 `skills/deep-research/SKILL.md` 最后 100 行，确认结构正确。

- [ ] **Step 4: Commit**

```bash
git add skills/deep-research/SKILL.md
git commit -m "feat(deep-research): 新增 Step 6 质量自检 — 自动检测报告结构/引用/假设/来源支撑"
```

---

### Task 2: 更新 backlog 状态

**Files:**
- Modify: `docs/backlog/2026-03-31-deep-research-quality.md`
- Modify: `docs/backlog/INDEX.md`

- [ ] **Step 1: 更新 backlog 文件中 P0 项状态**

在 `docs/backlog/2026-03-31-deep-research-quality.md` 中，将 P0 项的 `- [ ]` 改为 `- [x]`：

```markdown
- [x] **P0** 建立报告质量自动检测（结构完整性、引用格式、假设验证表、T1/T2 来源比例）
```

- [ ] **Step 2: Commit**

```bash
git add docs/backlog/2026-03-31-deep-research-quality.md
git commit -m "chore(backlog): 标记 deep-research 质量检测 P0 完成"
```
